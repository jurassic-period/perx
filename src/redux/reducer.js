export const reducer = (state = { page: 0 }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export const dilerReducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
